import lib.io

extern def main(argc: i32, argv: u8**): i32 {
  io <<< "CHECK: readonly data store\n"
  let data = "readonly" as usize as u8 volatile var*
  (*data) = 'x'
  io <<< "FAIL: rodata is writable\n"
  1
}
