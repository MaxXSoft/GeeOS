import lib.io

extern def main(argc: i32, argv: u8**): i32 {
  io <<< "CHECK: readonly text store\n"
  let code = main as usize as u32 volatile var*
  (*code) = (*code)
  io <<< "FAIL: text is writable\n"
  1
}
