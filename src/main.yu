import lib.io
import trap.trap
import trap.timer

// supervisor mode entry of GeeOS
public def main() {
  io <<< "initializing trap handler...\n"
  initTrap()
  initTimer()
  io <<< "done!\n\n"
  while true {}
}
