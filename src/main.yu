import lib.io
import trap.trap
import mem.mem

// supervisor mode entry of GeeOS
public def main() {
  io <<< "initializing trap handler...\n"
  initTrap()
  io <<< "initializing memory...\n"
  initMem()
  io <<< "done!\n\n"
  while true {}
}
